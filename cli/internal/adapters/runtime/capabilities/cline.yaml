adapter: cline
version: "1.0"
supports:
  - session.start
  - session.end
  - mcp.tools
experimental:
  - turn.complete
  - compact.triggered
  - clear.triggered
notes:
  - "Hooks: Cline VS Code extension only. TUI mode does not execute .clinerules/hooks/."
  - "Recording: Use mom_record_turn MCP tool as fallback in TUI mode."
